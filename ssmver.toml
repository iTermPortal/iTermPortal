version = "0.2.4"

[settings]
mode = "branch"
prompt = "never"
prompt_prefixes = [
    "feature",
    "release",
]

[sync]
monorepo = "lockstep"
constants = "curated"
exclude = []

[prefixes]
feature = "minor"
fix = "patch"
release = "major"

[release]
enabled = false
on_bump = []
match = ""
skip = ""
