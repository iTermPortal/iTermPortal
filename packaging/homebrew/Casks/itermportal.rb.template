cask "itermportal" do
  version "__VERSION__"
  sha256 "__SHA256__"

  url "https://github.com/__REPOSITORY__/releases/download/v#{version}/__ASSET_NAME__"
  name "iTermPortal"
  desc "Finder toolbar app that opens a terminal in the current folder"
  homepage "https://github.com/__REPOSITORY__"

  livecheck do
    url :url
    strategy :github_latest
  end

  depends_on macos: ">= :big_sur"

  app "iTermPortal.app"

  zap trash: [
    "~/Library/Application Support/iTermPortal",
    "~/Library/Preferences/com.hjoncour.fportal.plist",
    "~/Library/Preferences/com.hjoncour.fportal.menu.plist",
  ]
end
